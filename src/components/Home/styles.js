import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  noTodos: {
    minHeight: "67vh",
  },
  homeContainer: {
    backgroundColor: "#f2f2f2",
    width: "100%",
  },
}));
