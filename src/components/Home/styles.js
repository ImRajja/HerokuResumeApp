import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  noTodos: {
    minHeight: "67vh",
  },
  homeContainer: {
    backgroundColor: "gray",
    width: "100%",
  },
}));
